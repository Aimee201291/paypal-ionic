import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.paypalionic',
  appName: 'paypal-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;